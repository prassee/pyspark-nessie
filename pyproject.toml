[project]
name = "pyspark-nessie"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark[sql]>=4.0.0",
    "typer>=0.16.0",
]

[dependency-groups]
dev = [
    "chispa>=0.11.1",
    "pytest>=8.4.1",
]

[tool.uv.workspace]
members = [
    "data-publish-app",
]
